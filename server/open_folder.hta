<html>
<head>
<HTA:APPLICATION SHOWINTASKBAR="no" WINDOWSTATE="minimize" BORDER="none" />
<script language="VBScript">
Sub FixPath(p)
    If Len(p) > 2 And Mid(p, 1, 1) = "/" And Mid(p, 3, 1) = ":" Then
        FixPath = Mid(p, 2)
    Else
        FixPath = p
    End If
End Sub
Sub Window_OnLoad
    Dim fso, shell, dir
    Set fso = CreateObject("Scripting.FileSystemObject")
    Set shell = CreateObject("WScript.Shell")
    dir = FixPath(fso.GetParentFolderName(document.location.pathname))
    shell.Run "explorer.exe """ & dir & """", 1, True
    window.close()
End Sub
</script>
</head><body></body></html>