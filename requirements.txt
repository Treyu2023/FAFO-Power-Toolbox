# =============================================================================
# AI HTML Toolbox / FAFO Power Toolbox — Python dependencies
# Install into a LOCAL virtualenv (.venv) only — never require global pip mess.
#
# Supported Python: 3.10, 3.11, 3.12  (recommended: 3.12)
# Setup (Windows):
#   INSTALL-PYTHON.bat
#   or:  powershell -File Scripts\Install-PythonEnvironment.ps1
# =============================================================================

# --- Web backend (local loopback server) ---
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0

# --- Media / files ---
mutagen>=1.47.0
Pillow>=10.0.0

# --- System / tray / health ---
pystray>=0.19.0
psutil>=5.9.0

# --- Windows integration (Explorer tags, shell, etc.) ---
pywin32>=306; sys_platform == "win32"
